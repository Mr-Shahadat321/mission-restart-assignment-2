<!-- What is JSX, and why is it used?
What is the difference between State and Props?
What is the useState hook, and how does it work?
How can you share state between components in React?
How is event handling done in React? -->

1. JSX means javascript XML. Its allow to write HTML inside of JS react.

2. Props means pass data process from parent to child.
   AND
   state is used for to manage inside a component and it change over the time
   and than its re-render.

3. useState is called hook in react, It allows a component to remember values
   and update them.

4. state can be shared by lifting state. from child to parent react not allow
   so parent keep the state and pass to children.

5. event handling is done by attaching event handling function and performed
   according to the requirement, like onClick, onSubmit, onChange.  